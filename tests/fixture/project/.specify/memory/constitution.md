# Fixture Project Constitution

## Core Principles

### I. Single Owner

Every task MUST have exactly one owner. Shared or unassigned active tasks are not allowed.

### II. Explicit Data Retention

Every feature that stores user data MUST state its retention rule in the spec. Changing a retention rule MUST be recorded, never silent.

## Quality Gates

- All API routes MUST have automated tests before merge.
