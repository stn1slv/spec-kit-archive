# Fixture Project — Copilot Instructions

The project's real agent anchor. The last-resort probe never looks here, so a
run that writes to `AGENTS.md` instead has skipped the defaults lookup.

## Active Technologies

- Python 3.12 + FastAPI 0.115, SQLAlchemy 2.0 (001-task-manager)

## Recent Changes

- 001-task-manager: task CRUD, single-owner assignment, deadlines
