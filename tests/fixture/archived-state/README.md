# Archived-state overlay

The exact end state of the round-1 after-test run **C2** (features 001 and 002 archived at full scope by the v1.1.3 command text at `dc2287a`, supersessions confirmed: FR-004 → FR-009, SC-003 retired). This is the starting state Cases E and E2 pre-register their IDs against: `FR-001..003, FR-005..009` live, `FR-004`/`SC-003` retired, `AS-001..004`, Constraints holding "single region".

To use: copy a clean `project/`, replace its `.specify/memory/{spec,plan,changelog}.md` and `AGENTS.md` with these files (keep `constitution.md`), then archive `specs/003-reporting`.

Committed so the E/E2 baseline is reproducible by anyone, not only by the session that happened to hold this state on disk.
