# Fixture Project — Claude Instructions

A second context anchor at a nested, project-relative path. It carries the same
managed-section set as `AGENTS.md`, and has no marker block of its own yet.

## Active Technologies

- Python 3.12 + FastAPI 0.115, SQLAlchemy 2.0 (001-task-manager)

## Commands

- `make test` — run pytest

## Recent Changes

- specs/001-task-manager: task CRUD, single-owner assignment, deadlines
